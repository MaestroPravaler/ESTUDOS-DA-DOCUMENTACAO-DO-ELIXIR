# Sinais matemáticos básicos (Adição, Subtração, Multiplicação e Divisão)

# Adição
soma = 2 + 2
IO.puts(soma)

# Subtração
subtracao = 2 - 2
IO.puts(subtracao)

# Multiplicação
multiplicacao = 2 * 3
IO.puts(multiplicacao)

# Divisão
divisao = 2 / 3
IO.puts(divisao)

IO.puts(div(10, 2))

# Resto
IO.puts(rem(10, 3))
