# Strings sempre delimitada por aspas duplas.

IO.puts(is_binary("Uma String"))

# Contar o número de bytes
IO.puts(byte_size("Uma String"))

# Contar o número de caracteres incluindo os espaços.
IO.puts(String.length("Uma String"))

# Caixa alta
IO.puts(String.upcase("Uma String"))
